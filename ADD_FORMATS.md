# ADD_FORMATS.md — бэклог форматов «для 1%»

> Дополнительные конвертеры и генераторы — нишевые, но именно они выделяют UWC: большинство конкурентов останавливаются на популярном (99%), а про 1% забывают. Основной роадмап — в `TODO.md`; сюда вынесены идеи, которые не в основном списке форматов.
>
> **Правила те же:** если конвертер помещается в ~50–100 строк без зависимостей — делаем руками и в браузере (уровень A). Цены указаны по опыту уже сделанных форматов (TAR-парсер, IR-форматы и т.д.).

---

## 1. Текст — кодировки и шифры 💚 вечер каждая

| Идея | Почему «1%», но нужен | Цена |
|---|---|---|
| **Base32 / Base36 / Base62 / Base85 (Ascii85)** | Base64 есть; Base32 живёт в TOTP-секретах и DNS, Base62 — в short-ID (YouTube), Base85 — в git patches | вечер | ✅
| **Punycode / IDN** (`xn--80ak6aa92e` ↔ `пример.рф`) | фишинг-анализ, интернациональные домены — браузерный `URL` API делает почти всё за нас | вечер | ✅
| **Quoted-Printable** | старые email-заголовки и MIME | вечер | ✅
| **Uuencode** | ретро-форумы, ньюсгруппы | вечер | ✅
| **Морзе ↔ текст** | бренд-фича + радиолюбители | вечер | ✅
| **ROT47 / Atbash / Цезарь со сдвигом / XOR с ключом** | у нас только ROT13 — классика CTF-задач | вечер | ✅
| **A1Z26, шифр Бэкона, тап-код** | «бесполезные» в лучшем смысле бренда | час-вечер | ✅
| **EBCDIC ↔ ASCII** | мейнфреймы, банковские дампы — чистый 1% | день (таблица кодировки) | ✅
| **Baudot/ITA2** | телетайпы, RTTY | день | ✅
| **Full-width ↔ half-width** (日本語) | i18n-кейсы CJK | вечер | ✅
| **Убрать диакритику (NFD-strip)** | поиск, slug'и; почти бесплатен при готовом транслите | час | ✅

## 2. Текст — практичные мелочи, которых нет почти нигде 💚

| Идея | Зачем | Цена |
|---|---|---|
| **«Unfuck paste»** — убрать zero-width/invisible символы, умные кавычки → прямые, нормализация тире/многоточий | боль каждого, кто копирует из Word/PDF/Slack | вечер | ✅
| **Zero-width стеганография** — спрятать сообщение в невидимых символах / вытащить | киллер-фича бренда: «конвертер даже невидимого» | вечер | ✅
| **Строки: sort / dedupe / shuffle / нумерация / wrap на колонку N** | базовые операции, которых нет у половины конкурентов | вечер на всё | ✅
| **Unix timestamp ↔ ISO 8601** (s/ms, локальная/UTC) | самый частый инструмент «раз в месяц» у разработчика | вечер | ✅
| **ISO 8601 duration** (`PT1H30M` ↔ секунды) | спецификации, длительности YouTube | вечер | ✅
| **Числа: произвольная система счисления (2–36)** | вечная классика | вечер | ✅
| **Римские ↔ арабские** | классика | вечер | ✅
| **HEX ↔ RGB ↔ HSL** | цветовые коды как текст | вечер | ✅
| **MAC-адрес форматы** (Cisco `aabb.ccdd`, `:`, `-`) | сетевики скажут спасибо | час | ✅
| **JWT decode** (header.payload → JSON, без верификации) | дебаг токенов | вечер | ✅
| **Glob ↔ regex** | .gitignore, rsync-паттерны | вечер | ✅
| **C-style escapes** (`\n\t\x41` ↔ литералы) | вставка строк в код | вечер | ✅
| **Shell-quoting / argv-split** | экранирование для bash/cmd/PowerShell | вечер | ✅
| **Windows ↔ POSIX пути** | кроссплатформенные скрипты | час | ✅
| **CSV/TSV → Markdown-таблица** | документация, README | вечер | ✅
| **.env ↔ JSON** | конфиги | вечер | ✅
| **Java-properties ↔ JSON** | legacy-конфиги Java | вечер | ✅
| **HJSON ↔ JSON** | «человеческий JSON» (конфиги без кавычек) | вечер | ✅
| **JSON → TypeScript-интерфейсы** | разработчики расшаривают такие тулзы друг другу | день | ✅
| **BBCode → HTML** | ретро-форумы до сих пор живы | вечер | ✅
| **Zalgo: наложить/снять combining-марки** | фана + практичная «починка» текста | вечер | ✅
| **Slugify** (транслит + lowercase + дефисы) | почти бесплатен при готовом транслите | час | ✅

## 3. Генерация 🔑

| Идея | Зачем | Цена |
|---|---|---|
| **TOTP/HOTP** (RFC 6238): живой код 2FA из секрета + билдер `otpauth://` URI | киллер-фича: онлайн-2FA без телефона — почти никто не делает. WebCrypto HMAC | день | ✅
| **Snowflake ID**: генерация + декод таймстампа (Twitter/Discord epoch) | парсинг ID из API соцсетей | вечер | ✅
| **NanoID** | стандарт де-факто, ~10 строк рядом с ULID | час | ✅
| **KSUID** | Stripe/Segment-мир | день | ✅
| **Контрольные суммы: Luhn, ИНН, ISBN, EAN/UPC, CRC32** | валидация номеров карт/документов; ИНН — под RU-аудиторию | вечер-день | ✅ |
| **IBAN валидация/форматирование** | финтех | вечер | ✅
| **Chmod-калькулятор** (`755` ↔ `rwxr-xr-x`) | классика жанра «раз в полгода» | вечер | ✅
| **WiFi-QR билдер** (`WIFI:S:ssid;T:WPA;P:pass;;`) | гостям дома/офиса; сразу в существующую QR-вкладку | вечер | ✅
| **Lorem ipsum EN/RU + минимальный fake-data** (имена/email) | тестовые данные без faker-зависимости | вечер-день | ✅
| **JWT signer HS256** (WebCrypto) | пара к декодеру из раздела 2 | день | ✅
| **Кубик / монетка / random-строка из списка** | «бесполезное» в чистом виде, бренд | час | ✅

## 4. Топ-10 по ROI

1. **Unfuck paste** — единственный конвертер, который пригодится вообще каждому
2. **TOTP + otpauth-билдер** — «онлайн-2FA» это готовый заголовок для лендинга
3. **Unix timestamp ↔ ISO** — гарантированный поисковый трафик
4. **JSON → TS-интерфейсы** — разработчики расшаривают друг другу
5. **Zero-width стеганография** — уникальность, конкуренты не делают
6. **Base32/Base85** — закрывает TOTP-секреты и git-patches
7. **JWT decode** — частый сценарий, вечер работы
8. **Chmod-калькулятор** — вечнозелёный SEO-запрос
9. **WiFi-QR** — синергия с готовой QR-вкладкой
10. **Строки sort/dedupe/wrap** — дешёвый набор бытовых задач

## 5. Технические заметки для реализации

- **i18n**: все строки — в `i18n/locales/*.json`, реестры в `shared/` языконезависимы (см. грабли в TODO.md). Помнить про экранирование `@` (`{'@'}`).
- **WebCrypto** доступен и в браузере, и в Node 22 (глобально) — TOTP/HOTP/JWT signer тестируются в vitest без полифиллов.
- **TOTP**: HMAC-SHA1 через `crypto.subtle`, base32-декод секрета нужен свой (~30 строк) — см. Base32 из раздела 1, делаем его первым.
- **Zero-width стеганография**: кодировать биты через `\u200b/\u200c` (или ZWJ/ZWNJ для плотности) — декодер обязан быть строгим к «мусорным» невидимым символам, иначе конфликтует с «Unfuck paste» (там они удаляются — это фича, не баг: два разных конвертера).
- **Punycode**: `new URL()` + `URLSearchParams` покрывают IDN; для обратного преобразования — `URL.prototype.hostname` уже отдаёт punycode.
- **Римские числа**: диапазон 1–3999, классический greedy-алгоритм ~20 строк.
- **CRC32**: таблица 256 записей, ~25 строк; сверить с эталонным вектором `123456789` → `0xCBF43926`.
- **Luhn/ИНН/ISBN**: ИНН — два набора весов (10- и 12-значный), тесты на реальных валидных/невалидных номерах.
- **Glob → regex**: `*`, `?`, `**`, `[abc]`, `{a,b}` — начинать с простого подмножества, как в .gitignore.
